<?xml version="1.0" encoding="utf-8"?>
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
    android:background="@drawable/message_background">

        <ImageButton
            android:layout_width="wrap_content"
            android:id="@+id/replyButton"
            android:layout_height="wrap_content"
            android:background="@drawable/ic_send_black_24dp"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:onClick="postForumReply"/>

        <android.support.design.widget.TextInputEditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content" 
            android:layout_toLeftOf="@+id/replyButton"
            android:layout_toStartOf="@+id/replyButton"
            android:background="@drawable/bg_reply_edit"
            android:padding="10dp"
            android:id="@+id/repyField"
            android:layout_alignBaseline="@+id/replyButton"/>

    </RelativeLayout>
